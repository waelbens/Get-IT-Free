<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.melek.getitfree">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".inscrire" />
        <activity android:name=".tab_actuality" />
        <activity android:name=".tab_announcements" />
        <activity android:name=".tab_history" />
        <activity android:name=".show_actuality" />
        <activity android:name=".show_announcements" />
        <activity android:name=".add_announcement" />
        <activity android:name=".menu" />
        <activity android:name=".reset_password_activity" />
        <activity android:name=".tab_settings"></activity>
    </application>

</manifest>